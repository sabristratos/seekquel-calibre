## What changes

Describe what a reader would notice, not only what changed in the code.

## Why

## How it was tested

- [ ] `ruff check .` passes
- [ ] `python -m compileall -q seekquel_sync build.py` passes
- [ ] Installed the built zip and ran it against a real library
- [ ] Checked both directions: sent books, then read them back

## Notes

Anything a reviewer should know: a Calibre API this depends on, a column type it cannot
be tested against, a decision you were unsure about.
